//! The rmcp server: tools, auth layer, verdict mapping.

/// Run the daemon (MCP server on loopback) for `hatch serve`.
pub fn run_serve() -> anyhow::Result<()> {
    todo!("Task 20: rmcp server, tools, auth layer, verdict mapping")
}
