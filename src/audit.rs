//! `AuditRecord`, `LogVerdict`, append-only audit writer.

/// Tail the audit log for `hatch log`.
pub fn tail() -> anyhow::Result<()> {
    todo!("Task 3: audit record model and append-only writer")
}
